spring:
  application:
    name: order-service
---
spring:
  config:
    activate:
      on-profile: local
amazon:
  aws:
    access-key: 1
    secret-key: 2
  dynamodb:
    endpoint: http://localhost:4566/
dynamodb:
  tableName: order-table-local

---
spring:
  config:
    activate:
      on-profile: test
dynamodb:
  tableName: order-table-test

---
spring:
  config:
    activate:
      on-profile: prod
dynamodb:
  tableName: order-table-prod

